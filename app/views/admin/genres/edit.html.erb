<h2><div class="p-3 mb-2 bg-light text-dark">ジャンル編集</h2>
<%= form_with model: @genre, url:admin_genre_path(@genre.id), method: :patch do |f| %>



  <h4>ジャンル</h4>
  <%= f.text_field :name %>
  
 
  
  
  <%= f.submit '変更を保存',class: "btn btn-success" %>
  
<% end %>

